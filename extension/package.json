{
  "name": "extension",
  "version": "0.1.0",
  "description": "COMET Shortcuts Chrome Extension",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "tsc && vite build --mode production",
    "typecheck": "tsc --noEmit",
    "pack": "cd dist && zip -r ../extension.zip ."
  },
  "dependencies": {},
  "devDependencies": {
    "@types/chrome": "^0.0.254",
    "typescript": "^5.3.3",
    "vite": "^5.0.8"
  }
}
