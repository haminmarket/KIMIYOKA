# ⚠️ SECURITY WARNING:
# - NEVER commit this file with real credentials
# - NEVER put service role key here (backend only!)
# - Only public/anon keys are safe for extension builds
#
# Get credentials from: Supabase Dashboard → Settings → API

# Supabase Configuration
VITE_SUPABASE_PROJECT_REF=your-project-ref-here
VITE_SUPABASE_ANON_KEY=your-anon-key-here
VITE_SUPABASE_FUNCTION_BASE_URL=https://your-project-ref-here.supabase.co/functions/v1

# Environment (MUST be: development | staging | production)
VITE_APP_ENV=development

# Default Language (en | ko)
VITE_DEFAULT_LANG=en

# See docs/llm-context/SUPA_환경변수,스켈레톤,예시API.txt for details
